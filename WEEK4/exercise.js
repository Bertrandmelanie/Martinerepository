function calculateTip () {
    const bill= float(prompt("What is the amount of the bill"));
}
 let= tip (50)
 if (bill < 50) {
    tip = 0.2;
 }
 else if (bill >=50 && bill <= 200) {
    tip= .015;

 }
  else (bill >200){
    tip= 0.1;
  }

  const tipAmount = bill *  tip;
  const totalBill = bill + tipAmount

  console.log('Tip amount: $${')